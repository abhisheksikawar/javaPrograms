import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class RegExProgram {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		/*
		 * Pattern p= Pattern.compile("Abhishek"); Matcher m=p.matcher("ABHISHEK");
		 * System.out.println(m.matches());
		 * System.out.println(Pattern.matches("[A-Z]{8}", "ABHISHEK"));
		 */
		/*
		 * String text = "This is the text which is to be searched " +
		 * "for occurrences of the word is.";
		 * 
		 * String regex="is"; Pattern p=Pattern.compile(regex); Matcher
		 * m=p.matcher(text); System.out.println(m.matches()); int count=0;
		 * while(m.find()) { count++;
		 * 
		 * } System.out.println(count);
		 */
		
		
		
		
	}

}
